@{
    ViewData["Title"] = "Check Your Email";
}

<div class="auth-wrap">
    <div class="auth-card">
        <div class="card p-4 p-md-5 text-center">
            <div style="width:52px;height:52px;border-radius:50%;background:var(--accent-light);display:flex;align-items:center;justify-content:center;margin:0 auto 1.25rem">
                <i class="bi bi-envelope-check" style="color:var(--accent-color);font-size:1.4rem"></i>
            </div>
            <h2 class="h5 fw-bold mb-2">Check your email</h2>
            <p class="text-muted mb-4" style="font-size:14px">
                If an account matches that email address, we've sent a link to reset your password.
            </p>
            <a href="@Url.Action("Login", "Account")" class="btn btn-primary w-100">Back to Login</a>
        </div>
    </div>
</div>
